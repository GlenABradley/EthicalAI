// Simple setup file for tests
import '@testing-library/jest-dom/vitest';

// No additional setup needed as @testing-library/jest-dom/vitest
// automatically extends Vitest's expect with custom matchers
